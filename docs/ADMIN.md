# Admin — Acervo Acadêmico Inteligente

Este documento descreve a configuração e uso do painel de administração e dos endpoints server-side.

Variáveis de ambiente necessárias (defina na Vercel/ambiente):

- `ADMIN_PASSWORD` — senha do administrador (string secreta)
- `ADMIN_JWT_SECRET` — segredo para assinar JWTs (string forte)
- `NEXT_PUBLIC_SUPABASE_URL` — URL do Supabase (ex: https://xyz.supabase.co)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` — chave anon (client)
- `SUPABASE_SERVICE_ROLE_KEY` — chave service role (muito secreta)
- `GROQ_API_KEY` — chave da Groq (opcional; pode ser substituída via painel)

Observações de segurança:
- Nunca use `NEXT_PUBLIC_` para segredos sensíveis; use `SUPABASE_SERVICE_ROLE_KEY` e `ADMIN_JWT_SECRET` somente no servidor.
- Marque cookies como `Secure` em produção (Vercel). O endpoint `admin_login` adiciona cookie `admin_token` HttpOnly.

Banco de dados — migração mínima

Execute no SQL editor do Supabase:

```
create table if not exists documentos (
  id bigint generated by default as identity primary key,
  titulo text,
  drive_url text,
  conteudo_snippet text,
  tags text[],
  created_at timestamptz default now()
);

create table if not exists settings (
  id uuid default gen_random_uuid() primary key,
  key text unique not null,
  value text,
  inserted_at timestamptz default now()
);
```

Como funciona:
- `POST /api/admin_login` — recebe `{ password }`, valida contra `ADMIN_PASSWORD` e seta cookie HttpOnly `admin_token` com JWT curto.
- `GET /api/admin_docs` — exige autenticação JWT; pode usar query params:
  - `page` (1), `per_page` (10)
  - `tag` (filtra por tag)
  - `search` (pesquisa no `titulo`)
  - `distinct_tags=1` retorna lista de tags distintas
- `PUT /api/admin_docs` — atualiza documento enviando `{ id, titulo?, conteudo_snippet?, tags? }` no corpo
- `DELETE /api/admin_docs` — exclui documento enviando `{ id }` no corpo
- `POST /api/process-link` — exige JWT admin; baixa o Google Doc, chama Groq e insere no Supabase usando `SUPABASE_SERVICE_ROLE_KEY`.
- `POST /api/admin_set_groq_key` — permite gravar uma chave Groq alternativa na tabela `settings`.

Testes rápidos (local):

1. Defina as env vars localmente (ou no `.env`) para testes.
2. Rode o Next.js local (`npm run dev`) e garanta que as Python serverless functions estejam disponíveis no ambiente de deploy (Vercel instala `requirements.txt`).
3. Acesse `/admin`, faça login com `ADMIN_PASSWORD` e teste adicionar/editar/excluir.

Notas finais:
- Recomenda-se configurar logs de auditoria para ações admin.
- Para grande volume de documentos, ajuste paginação e índices no Supabase.

